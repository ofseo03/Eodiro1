- 목표: 서울 내 코스 추천 웹 서비스
- 특징: 옵션에 따른 코스 추천 기능 제공
    - 날씨 반영 
        - 덥거나 춥거나 비 예보가 있는 경우, 실내 코스 추천
    - 총 이동 시간 설정
    - 가능한 교통수단 선택: 버스, 지하철, 도보(코스 최대 도보 거리 설정/예: 500m), 택시(코스 간 교통수단 경로가 없는 경우.)
    - 코스 추천받을 구체적인 장소 선택

- 사용 기술 및 api:
    - 비짓서울 api
    - 서울 열린 데이터 api
    - 기상청 단기예보 조회서비스 api
    - 서울시 대중교통환승경로 API 
    - 서울교통공사 최단경로 API

---

# 상세 서비스 스펙

## 1. 서비스 범위와 용어

- 사용자가 선택한 서울 내 지역(예: 성수) 안에서 카페·식당·놀거리를 묶은 방문 코스를 추천한다. 여러 지역을 순회하는 일정이 아니다.
- 처음 설정한 취향을 이후 추천에 재사용하고, 선택 지역과 날씨에 맞춰 장소를 구성한다.
- 위 원문은 최초 작성 내용을 보존한 것이며, 구체적인 동작은 아래 상세 스펙을 기준으로 한다.
- 코스는 순서가 있는 방문 장소들의 목록이며, 이동 구간은 연속한 두 장소 사이를 뜻한다.
- 최대 도보 거리는 **이동 구간별 기준**이며, 사용자가 설정할 수 있는 상한은 1km(1,000m)이다. 전체 코스의 누적 도보 거리 제한을 뜻하지 않는다. 기본값은 미정이다.
- 공공 API를 우선 사용한다. 문서상 제공 기능과 실제 호출로 검증한 기능을 구분한다.
- 택시 없이 완성되는 코스를 우선 추천한다. 필요할 때 택시 대안을 추가하며, 호출·예약 기능은 포함하지 않는다.

## 2. 사용자 입력

| 항목 | 내용 |
| --- | --- |
| 초기 취향 | 최초 설정 후 재사용한다. 구체적인 취향 항목과 저장 방식은 추가 확정한다. |
| 추천 지역 | 성수 등 서울 내 지역 선택. 해당 지역 안에서 방문 장소를 구성한다. |
| 총 이동시간 상한 | 사용자가 허용하는 이동시간. 관광·식사 등 체류시간을 제외한 구간별 이동시간의 합을 기준으로 한다. |
| 허용 교통수단 | 버스·지하철·도보를 기본 추천 대상으로 하며 사용자 선택을 따른다. 택시는 필요 시 대안으로 추가한다. |
| 구간별 최대 도보 거리 | m 단위로 설정하며 최대 1,000m까지 허용한다. 기본값은 미정이다. |

- 출발 위치와 방문 날짜·시간의 입력 방식은 구현 전에 확정한다. 날씨와 경로 계산에 사용한 기준 위치·시각을 결과에 표시한다.
- 시간·거리 입력은 양수여야 하며, 도보 거리 입력은 1,000m 이하여야 한다. 허용 교통수단을 하나 이상 선택해야 한다.

## 3. 추천 처리

1. 선택 지역 안에서 초기 취향에 맞는 카페·식당·놀거리 후보를 조회한다.
2. 방문 시점에 해당하는 날씨를 확인하고, 덥거나 춥거나 비 예보가 있으면 실내 장소를 우선 추천한다.
3. 택시 없이 이동할 수 있는 방문 순서를 우선 구성하고, 각 이동 구간의 허용 교통수단과 예상 이동시간을 확인한다.
4. 구간별 최대 도보 거리와 총 이동시간 상한을 적용한다.
5. 방문 순서, 구간별 이동방법, 거리·시간 및 미확인 정보를 함께 제공한다.

### 날씨와 실내 장소

- 덥거나 춥다고 판단할 기온 기준과 비 예보 판단 기준은 구현 전에 확정한다.
- 장소는 실내·실외·복합·미확인으로 구분한다. API가 구분 정보를 제공하지 않으면 확인한 장소부터 분류를 보완한다.
- 복합·미확인 장소를 실내로 단정하지 않는다.
- 날씨 조회 실패 또는 예보 범위 밖인 경우에는 '날씨 미반영'을 표시한다.

### 이동수단 결정

- 도보가 허용되고 실제 보행거리가 설정값 이하이면 도보를 후보에 포함한다.
- 도보 거리를 초과하거나 도보가 허용되지 않으면 선택한 버스·지하철 수단으로 경로를 조회한다.
- 대중교통 경로에 사용자가 허용하지 않은 승차 수단이 포함되면 해당 경로를 제외한다.
- 정류장·역 접근 및 환승에 필요한 도보도 별도로 표시한다. 이 도보에 거리 제한을 적용하는 방식과 도보 미선택 시 허용 여부는 구현 전에 확정한다.
- 택시 없는 대체 코스를 우선 찾는다. 그래도 허용 조건에 맞는 경로가 없고 사용자가 택시 대안을 허용하면 '택시 이용 검토 · 소요시간 미확인'으로 표시한다.
- 택시도 허용되지 않으면 해당 구간을 이동 가능한 것으로 처리하지 않고, 다른 코스를 찾거나 조건에 맞는 코스가 없음을 안내한다.
- API 오류·시간 초과는 '경로 조회 실패'로 표시한다. 정상 조회 결과의 '경로 없음'과 구분하며, 오류만으로 택시 대안으로 전환하지 않는다.

### 거리·시간의 정확성

- 직선거리는 실제 보행거리와 구분한다. 직선거리만 확보한 경우 '직선거리 기준 · 실제 보행거리 미확인'으로 표시한다.
- 실제 보행거리가 미확인인 구간은 최대 도보 거리 충족을 확정하지 않는다.
- 택시 또는 도보 등 일부 구간의 시간이 미확인이라면, 알려진 시간의 합은 '확인된 구간 이동시간 합계'로 표시한다.
- 미확인 시간을 0분으로 처리하지 않으며, 해당 코스를 '총 이동시간 상한 충족'으로 표시하지 않는다.
- 대중교통 API의 소요시간에 접근 도보·환승·대기가 포함되는지 확인하여 누락과 중복 합산을 방지한다.

### 장소 하나 교체하기

- 먼저 완성된 코스를 제공하고, 사용자는 그중 한 장소를 선택해 교체 후보를 요청한다.
- 예: 카페 A를 바꾸려면 **카페 A 기준 반경 100m 내 같은 카테고리의 다른 장소**를 추천한다.
- 100m 반경은 좌표 기준 직선거리로 해석한다. 실제 보행거리 제한과는 별도로 적용하며, 거리 기준은 최종 확인한다.
- 원래 장소와 코스에 이미 포함된 장소는 교체 후보에서 제외한다. 선택 지역과 기존 취향·날씨 조건도 유지한다.
- 교체 후보를 고르면 해당 장소만 바꾸고 나머지 장소와 방문 순서는 유지한다.
- 교체 장소의 앞뒤 이동 구간을 다시 조회하여 구간별 도보 거리와 총 이동시간 상한을 재검증한다. 조건을 벗어나면 조건에 맞는 교체로 확정하지 않는다.
- 반경 내 후보가 없을 때의 처리는 추가 확정한다. 현재는 반경을 임의로 확대하는 것을 확정 요구사항에 포함하지 않는다.

## 4. 추천 결과

- 방문 장소와 방문 순서
- 각 장소의 실내·실외 구분과 날씨 반영 여부
- 구간별 이동수단, 노선·환승 정보, 확보된 거리와 예상 이동시간
- 총 예상 이동시간 또는 확인된 구간 이동시간 합계
- 도보 거리·이동시간 조건의 충족 여부와 판단할 수 없는 구간
- 경로 없음, 경로 조회 실패, 택시 대안 등 구간별 상태

## 5. API 구성

| API 또는 데이터 | 역할 | 채택 전 확인할 사항 |
| --- | --- | --- |
| Visit Seoul 기반 서울시 관광 문화 데이터 | 방문 후보의 장소명·주소·이용시간 등 | 사용할 실제 API, 좌표·운영정보 누락 여부, 실내·실외 분류 제공 여부 |
| 서울시 실시간 도시데이터 | 제공 대상 장소의 날씨 등 보조 정보 | 주요 121개 장소의 제공 범위와 추천 장소의 대응 관계 |
| 기상청_단기예보 조회서비스 | 방문 지역·시간대의 날씨 예보 | 위치의 예보 격자 변환, 발표 시각과 예보 가능 기간 |
| 서울특별시_대중교통환승경로 조회 서비스 | 버스 및 버스·지하철 환승 경로 | 실제 응답, 소요시간·도보 구간 필드, 수단별 조회 범위 |
| 서울교통공사_최단경로이동정보 | 지하철 역 간 경로와 이동시간 | 지원 역·노선, 역 식별자, 시간표 및 소요시간의 의미 |
| 스마트서울맵 OpenAPI — 필요 시 추가 | 주소·좌표 변환, 지도 표시 | V5 공식 목차에는 일반 POI 검색이 명시되지 않으므로 장소 검색 제공을 전제하지 않는다. 적용 버전과 실제 기능을 검증한다. |

- 현재 목록만으로 장소 간 전체 도보 경로와 실제 보행거리를 제공할 수 있는지는 미확인이다. 최대 도보 거리 기능의 핵심 검증 항목으로 둔다.
- 현재 목록에 택시 경로·시간 계산 API는 포함하지 않는다.
- 100m 내 같은 카테고리의 대체 장소를 찾으려면 장소별 좌표와 카테고리가 필요하다. 공공 관광 데이터가 해당 지역의 카페·식당·놀거리를 얼마나 포함하는지 실제 데이터로 확인한다. 후보가 항상 존재한다고 보장하지 않는다.
- 각 API의 실제 사용 가능 여부, 인증 방식, 호출 한도 및 출처 표시 조건을 확인한다.

공식 참고 문서:

- [서울시 관광 문화](https://data.seoul.go.kr/dataList/OA-21052/S/1/datasetView.do)
- [서울시 실시간 도시데이터](https://data.seoul.go.kr/dataList/OA-21285/A/1/datasetView.do)
- [기상청_단기예보 조회서비스](https://www.data.go.kr/data/15084084/openapi.do)
- [서울특별시_대중교통환승경로 조회 서비스](https://www.data.go.kr/data/15000414/openapi.do)
- [서울교통공사_최단경로이동정보](https://www.data.go.kr/data/15143842/openapi.do)
- [스마트서울맵 OpenAPI](https://map.seoul.go.kr/smgis2/openApi)
- [스마트서울맵 V5 연동 안내](https://map.seoul.go.kr/smgis2/division/viewOpenApi)

## 6. 완료 판단 기준

- 사용자에게 보여주는 추천 코스의 방문 장소가 선택 지역 안에 속한다.
- 처음 설정한 취향을 이후 지역별 추천에도 반영한다.
- 택시 없는 코스를 우선 추천한다.
- 장소 교체 후보는 기존 장소를 제외한 같은 카테고리이며, 기존 장소 기준 반경 100m 이내여야 한다.
- 장소 교체 후에도 앞뒤 이동 구간과 전체 이동시간 조건을 다시 확인한다.
- 허용하지 않은 교통수단을 이용 가능한 경로로 추천하지 않는다.
- 실제 보행거리가 설정값과 같으면 도보 후보에 포함하고, 초과하면 제외한다.
- 실내 분류와 해당 시간대 날씨가 확인된 경우에만 날씨를 반영한 실내 추천으로 표시한다.
- 경로 없음과 API 조회 실패를 구분한다.
- 일부 구간의 거리·시간이 미확인인 코스를 모든 조건이 충족된 코스로 표시하지 않는다.
- 실제 API 호출 결과로 장소 조회와 구간별 경로·시간 연결을 검증한 후, 검증한 교통수단 범위만 구현 완료로 판단한다.

## 7. 추가로 확정할 사항

- 초기 취향의 선택 항목과 저장 방식: 기기 내 저장 또는 계정 기반 저장
- 한 코스의 장소 수와 카테고리 구성: 고정 구성 또는 사용자 선택
- 성수 등 추천 지역의 경계 정의와 출발 위치·방문 시각 입력 방식
- 100m 내 교체 후보가 없을 때 안내만 할지, 사용자 선택으로 검색 범위를 넓힐지
- 정류장 접근·환승 도보에 1km 상한을 적용하는 방식
